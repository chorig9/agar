package com.game.agar.entities;

import com.game.agar.tools.Position;

public class Ball extends Entity {
    public Ball(Position position, int weight) {
        super(position,weight);
    }
}
